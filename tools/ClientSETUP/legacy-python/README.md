Legacy Python backup of the blue.dll patcher.

These files were preserved before the experimental native PowerShell patcher became the default launcher path.

Current default entrypoint:

- `tools/ClientSETUP/blue_dll_patch.ps1`
